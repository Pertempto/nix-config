# Example `wifi-secrets.nix` for hosts/usbSandbox
# Copy this to `wifi-secrets.nix` and replace placeholders with real values.
# The actual `wifi-secrets.nix` is gitignored and should never be committed.

{
  networks = {
    # Home network example
    "MyHomeSSID" = {
      psk = "your_home_password";
    };

    # Work network example
    "WorkSSID" = {
      psk = "your_work_password";
    };

    # For networks that require no password (open networks), use an empty attrset:
    # "GuestSSID" = {};
  };
}
