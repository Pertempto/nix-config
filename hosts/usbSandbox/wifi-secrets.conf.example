# Example wpa_supplicant-style `wifi-secrets.conf` for hosts/usbSandbox
# Copy to `wifi-secrets.conf` (gitignored) and fill in your credentials.

ctrl_interface=DIR=/run/wpa_supplicant GROUP=wheel
update_config=1

network={
  ssid="MySSID"
  psk="your_password"
}
